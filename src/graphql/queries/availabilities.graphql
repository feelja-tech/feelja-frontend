query availabilities {
  currentUser {
    id
    state
    matches {
      id
      availabilities
    }
    videoCalls {
      id
      availabilities
      profiles {
        id
      }
    }
  }
}

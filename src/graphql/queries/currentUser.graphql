query currentUser {
  currentUser {
    id
    state
    profile {
      propicFileDownloadUrl
    }
    videoCalls {
      initiator
      happensAt
      availabilities
    }
    meetings {
      happensAt
    }
  }
}

import React, { ReactElement } from "react";
import { LayoutStory } from "../../Layout.story";

export const Main = (): ReactElement => <LayoutStory />;

export default {
  title: "Pages/Main/RateDate",
  component: Main,
};
